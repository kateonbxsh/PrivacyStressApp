# NeuroMove

Web UI for the NeuroMove project.

## Stack
Python
- NiceGUI
- FastAPI (under the hood via NiceGUI)
- Mock-data-first architecture

## Run

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python -m app.main
```

Open http://localhost:8080
